{{#each nota}}
    <div class = "card">
        <div class = "card-body">
            <h3>{{titulo}}</h3>
            <small>categoria: {{categoria.nome}}</small>
            <hr>
            <br>
            <p>{{conteudo}}</p>
            <br>
            <hr>
            <small>Data: {{data}}</small>
        </div>
    </div>
{{else}}
{{/each}}